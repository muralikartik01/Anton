{
  "name": "cert-generator",
  "private": true,
  "version": "1.0.0",
  "description": "Generates ANTON IO internship certificates (HTML + PDF) from an Excel sheet of candidates.",
  "main": "generate-certificates.js",
  "scripts": {
    "generate": "node generate-certificates.js"
  },
  "dependencies": {
    "puppeteer": "^23.0.0",
    "xlsx": "^0.18.5"
  }
}
